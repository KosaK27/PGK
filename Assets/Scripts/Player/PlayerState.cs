public enum PlayerLocomotionState { Idle, Walk }
public enum PlayerAirState { Grounded, Jumping }
public enum PlayerActionState { None, UsingTool, UsingWeapon, Dead }

public enum PlayerState
{
    Idle,
    Walk,
    Jump,
    Dash,
    UseTool,
    UseWeapon,
    Dead
}